#!/usr/bin/env bash

RAILS_ENV=<%= defined?(environment) ? environment : 'production' %> bundle exec rake assets:precompile
RAILS_ENV=<%= defined?(environment) ? environment : 'production' %> bundle exec rake db:setup
RAILS_ENV=<%= defined?(environment) ? environment : 'production' %> bundle exec rake db:migrate
RAILS_ENV=<%= defined?(environment) ? environment : 'production' %> bundle exec rails server -b 0.0.0.0 -p <%= defined?(application_port) ? application_port : 5000 %>
